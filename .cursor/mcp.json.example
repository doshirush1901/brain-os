{
  "mcpServers": {
    "brain-os": {
      "command": "poetry",
      "args": ["run", "brain-mcp"],
      "cwd": "${workspaceFolder}",
      "env": {
        "BRAIN_DATA_DIR": "${workspaceFolder}/data"
      }
    }
  }
}
